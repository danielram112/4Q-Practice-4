Function OPENUP(){
    let carCounter = 18;
    const maxCars = 25;

    while(carCounter < maxCars) {
        window.alert( "The Car" + carCounter + "has entered");
        carCounter += 1;
    }
    
    Window.alert("Fullparking");
} 